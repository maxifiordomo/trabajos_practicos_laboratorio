/** \brief Funcion para Suma.
 *
 * \param float Ingreso de la variable numeroUno.
 * \param float Ingreso de la variable numeroDos.
 * \return float Retorno del resultado.
 *
 */
float calculoSuma(float ,float);

/** \brief Funcion para resta.
 *
 * \param float Ingreso de la variable numeroDos.
 * \param float Ingreso de la variable numeroDos.
 * \return float Retorno del resultado.
 *
 */
float calculoResta(float ,float);

/** \brief Funcion para multiplicacion.
 *
 * \param float Ingreso de la variable numeroUno.
 * \param float Ingreso de la variable numeroDos.
 * \return float Retorno del resultado.
 *
 */
float calculaMultiplicacion(float ,float);

/** \brief Funcion para division, la verificacion para division con 0 se realiza en la funcion main.
 *
 * \param float Ingreso de la variable numeroUno.
 * \param float Ingreso de la variable numeroDos.
 * \return float Retorno del resultado.
 *
 */
float calculoDivision(float ,float);

/** \brief Funcion factoriales, la verificacion para numero negativos se realiza en la funcion main y se desestiman los decimales tomandolos como enteros.
 *
 * \param int Ingreso de la variable(primero va ingresar la variable numeroUno y segundo numeroDos).
 * \return int Retorno del resultado(primero va retornar el resultado de la variable numeroUno y segundo numeroDos).
 *
 */
int factorial(int);
